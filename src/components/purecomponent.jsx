import React from 'react'

const Purecomponent = () => {
  return (
    <div>
        <h1>Pure Component</h1>
        <p>A Pure Component is a component that performs a check on the value of React props before deciding whether to re-render the component or not.A Pure function is a function that does not modify any external variable. And the Impure function modifies the external variable.</p>
    </div>
    
  )
}

export default Purecomponent